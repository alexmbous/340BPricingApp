# Metro (used by Expo/React Native) doesn't follow pnpm's symlinked node_modules
# layout. `node-linker=hoisted` gives us a classic flat node_modules that Metro
# can resolve. This is Expo's recommended setting in a pnpm monorepo.
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
